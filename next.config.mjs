/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // FIXTHIS renders no remote imagery; product links are text-only.
    remotePatterns: [],
  },
}

export default nextConfig
